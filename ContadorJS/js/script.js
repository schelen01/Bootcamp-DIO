
/* o span é o currentNumberWrapper*/
var currentNumberWrapper = document.getElementById('currentNumber');
/* é a nova variável que inicia a opreação*/
var currentNumber = 0;

function increment () {
    currentNumber = currentNumber + 1;
    currentNumberWrapper.innerHTML = currentNumber; /* innerHTML é o HTML que está dentro do currentNumberWrapper*/

}

function decrement () {
    currentNumber = currentNumber - 1;
    currentNumberWrapper.innerHTML = currentNumber;
}

function test () {
    kdowkdpo;
}